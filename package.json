{
    "name": "comida-cubana",
    "version": "1.0.0",
    "description": "Plataforma de entrega de comida cubana",
    "private": true,
    "scripts": {
        "backend": "npm run dev --prefix backend",
        "frontend": "npm run dev --prefix frontend",
        "dev": "npm-run-all --parallel backend frontend",
        "build": "npm run build --prefix frontend && npm run build --prefix backend",
        "start": "npm-run-all --parallel \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
        "lint": "npm run lint --prefix frontend && npm run lint --prefix backend"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}